import React, { memo } from 'react'
import { Layout } from 'antd'

const Footer = () => (
  <Layout.Footer style={{ textAlign: 'center', flex: '1 0 auto' }}>Footer here</Layout.Footer>
)

export default memo(Footer)
